# FilamentDB Environment Variables

# API URL for Native Apps (Android/iOS)
# When running the mobile app, it needs to know where your server is deployed
# Examples:
#   - Local development: http://192.168.1.100:3000 (your local IP)
#   - Production: https://your-filamentdb-server.com
# For web deployments, this is not needed (uses relative URLs)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Server-Side Sync API Key
# This secret key secures your sync endpoint. It must match the client's API Key setting.
# Defaults to 'dev-key-change-in-production' if not set.
# Recommended: Generate a secure random string (e.g. `openssl rand -hex 32`)
SYNC_API_KEY=your-secret-api-key-here
